# 🎨 دليل تجربة المستخدم وسير العمل (UI/UX & Workflow Guide)

هذا الدليل يشرح الفلسفة التصميمية لتطبيق "سكرك مضبوط" وكيفية تفاعل المستخدم مع الشاشات والتدفق البرمجي للوظائف الرئيسية.

---

## 1. الفلسفة التصميمية (Design Language)

### أ. الهوية البصرية
- **النمط:** يعتمد التطبيق على نمط "العناصر الطافية" (Card-based Design) مع زوايا مستديرة (20.0 Radius) لإعطاء شعور بالراحة والحداثة.
- **التفاعل:** يتم استخدام "Staggered Grids" (شبكات متباينة الأطوال) في الشاشات الرئيسية لعرض الخدمات بشكل غير تقليدي وجذاب.
- **الألوان:** استخدام الألوان الهادئة (Canvas Color) للخلفيات لتقليل إجهاد العين، مع ألوان حيوية للأيقونات لتمييز الخدمات بسهولة.

### ب. العناصر المتكررة (Standard Components)
1.  **SectionItem:** المكون الأساسي لعرض الأقسام، يحتوي على أيقونة مصورة، عنوان نصي، وظل خفيف (Elevation).
2.  **AppBars:** شريط علوي بسيط يعرض عنوان الصفحة مع زر "الرجوع" أو أيقونات التنبيهات والسلة.
3.  **Loading States:** استخدام `ModalProgressHUD` لإظهار مؤشر تحميل شفاف فوق المحتوى لضمان عدم تجمد الشاشة أثناء جلب البيانات.

---

## 2. سير العمل الرئيسي (Core Workflows)

### أولاً: رحلة البداية والتسجيل (Onboarding & Auth)
1.  **تبدأ الرحلة** بشاشة الـ Splash التي تتحقق من وجود تحديثات وتفحص حالة الجلسة.
2.  **في حال عدم التسجيل:** ينتقل المستخدم لشاشة تسجيل الدخول (رقم الهاتف + كلمة المرور).
3.  **إكمال الملف:** بعد الدخول لأول مرة، يطلب التطبيق بيانات السن والوزن والطول لبناء "الملف الصحي" الأولي.

### ثانياً: سير عمل تتبع الصحة (Health Tracking Flow)
1.  **الدخول:** من الشاشة الرئيسية -> "تابع صحتك".
2.  **الاختيار:** يختار المستخدم نوع التسجيل (سكر، رياضة، وجبات، أدوية).
3.  **الإدخال:** يفتح نموذج إدخال (Bottom Sheet أو شاشة جديدة) لإضافة القيمة والوقت.
4.  **الحفظ:** يتم الحفظ فوراً في قاعدة البيانات المحلية (SQLite) وتظهر القراءة في شاشة "السجل".

### ثالثاً: سير عمل السوق والمتجر (Market Flow)
1.  **التصفح:** شاشة المتجر تعرض بنرات للعروض، تليها شبكة من تصنيفات المنتجات.
2.  **البحث:** يمكن للمستخدم البحث الفوري عن أي منتج وتظهر النتائج في قائمة منسدلة ذكية.
3.  **السلة:** إضافة المنتجات للسلة -> مراجعة الكميات -> الانتقال لشاشة الدفع.
4.  **الدفع:** اختيار طريقة الدفع (كاش أو إلكتروني عبر XPay) ثم تأكيد الطلب.

### رابعاً: المواعيد والخدمات الطبية (Service Booking)
1.  **اختيار الخدمة:** (تحاليل، تمريض، أو استشارة).
2.  **النموذج:** ملء نموذج الطلب (التاريخ، الوقت، والموقع الجغرافي).
3.  **التأكيد:** يتم إسال الطلب للسيرفر ويظهر في تبويب "المواعيد" لمتابعته.

---

## 3. خريطة الشاشات التفصيلية (Visual Mapping)

| الشاشة | العناصر المرئية | الوظيفة البرمجية |
| :--- | :--- | :--- |
| **الرئيسية (Home)** | Banners, Location Bar, Sections Grid | المدخل الرئيسي والاكتشاف |
| **التسوق (Market)** | Search Bar, Product Categories, Cart Icon | تجارة إلكترونية |
| **تابع صحتك** | Grid Icons (Staggered), Insight Cards | إدخال بيانات حيوية |
| **السجل (Reports)** | Graphical Charts, History Lists | تحليل البيانات وعرضها |
| **الملف الطبي** | Forms, Checkboxes, Dropdowns | التقييم الصحي الشامل |

---

## 4. نظام التنقل (Navigation System)
- **التبويب السفلي (Bottom NavBar):** ينسق الانتقال بين (الرئيسية، السجل، المواعيد، الحساب).
- **التنقل العميق (Deep Navigation):** يتم استخدام `TabNavigationService` للانتقال من أي مكان في التطبيق إلى شاشة محددة (مثل النقر على إشعار يفتح صفحة الطلب مباشرة).

هذا الدليل يضمن لأي مطور فهم "روح" التطبيق قبل البدء في كتابة سطر كود واحد، مما يحافظ على اتساق التجربة للمستخدم النهائي.
